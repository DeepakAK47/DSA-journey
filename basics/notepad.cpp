1. basis==>patternPrinting==>Aray&Vector==>Sorting==>binarySearch==>Recursion==>specialAlgorithm==>OOPs==>LinkedList==>stack
Doubt prog48
Q. Whcih function is used for determing the size of datatype?
ANS. sizeof(int)

Q. Can we swap two number using function by passing their value using temp?
ANS. No, because when you call then two differnt variables will be created.

Q. WAP for finding the ASCII value of english albhabets.
ANS. int i = 65;
     char ch = (char)i;

Q. Write the syntax for using this operator.
ANS. condition ? if true : else this     

Q. Write the syntax to determine the integral value of character.
ANS. char ch = 'A';
     int i = (int)ch;

Q. What is type casting?
ANS. The conversion of one datatype to another is called type casting.

Q. 