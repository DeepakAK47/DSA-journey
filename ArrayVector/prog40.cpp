// Calculating the trap water between the building
#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
int main(){
    vector<int>v;
    int n;
    cout<<"Enter the size of the vector : ";
    cin>>n;
    cout<<"Enter the element of the vector : ";
    for(int i = 0;i<n;i++){
        int x;
        cin>>x;
        v.push_back(x);
    }
    cout<<"All the element of the vector are : ";
    for(int i = 0;i<n;i++){
        cout<<v[i]<<" ";
    }
    vector<int>arr;
    for(int i = 0;i<n;i++){
        
    }
    return 0;
}